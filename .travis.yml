language: python
python:
  - "3.6"
# command to install dependencies
install:
  - apt install python-dev swig libxml2-dev zlib1g-dev libgmp-dev
  - pip install pytest
  - pip install -e .
# command to run tests
script:
  - python -m pytest
